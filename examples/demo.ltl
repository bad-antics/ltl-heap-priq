// examples/demo.ltl — runnable demo for ltl_heap_priq

import std::io
use ltl_heap_priq as lib

fn main() {
    io::println("ltl_heap_priq demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}