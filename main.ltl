// ltl-heap-priq/main.ltl
//
// d-ary heap priority queue with decrease-key support.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-heap-priq v0.1.0")
    io::println("d-ary heap priority queue with decrease-key support.")
    0
}
